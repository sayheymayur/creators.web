$kind: http-request
name: Login
url: "{{base_url}}/auth/login"
method: POST
headers:
  Content-Type: application/json
body:
  type: json
  content: |-
    {
      "email": "abhaydemo4@gmail.com",
      "password": "password123"
    }
scripts:
  - type: afterResponse
    code: "const res = pm.response.json();\r

      pm.environment.set(\"token\", res.token);"
    language: text/javascript
order: 2000
